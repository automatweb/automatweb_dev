site_id = {VAR:site_id}

site_basedir = {VAR:basedir}
tpldir = ${site_basedir}/templates
cache.page_cache = ${site_basedir}/pagecache

baseurl = {VAR:baseurl}

default_user = {VAR:default_user}
default_pass = {VAR:default_pass}

db.user = {VAR:db_user}
db.pass = {VAR:db_pass}
db.host = {VAR:db_host}
db.base = {VAR:db_base}


# siin tuleks numbrid 6igeks ka panna - vastavalt sellele kuidas menyyd tehakse 2ra
groups.all_users_grp = 2
menuedit.menu_defs[9] = YLEMINE
rootmenu = 6
admin_rootmenu2 = 4
frontpage = 6
amenustart = 5

# nujah, seda v6iks ka ju kysida tegelt
stitle = Autom@web

# use MD5 passwords
auth.md5_passwords = 1
