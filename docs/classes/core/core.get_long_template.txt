function get_long_template($section)

Returns the document template filename for a single document under the specified folder.

<i>Parameters:</i>

$section - 
The folder for what to return the template. If no template is specified for that menu, the parent's template is returned. If no template is specified anywhere, defaults to "plain.tpl"